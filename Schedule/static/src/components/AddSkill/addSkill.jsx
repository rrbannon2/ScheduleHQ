import React from 'react';
import SkillPage from '../SkillPage/SkillPage';

const AddSkillPage = () => {
    return (
        <SkillPage addNew={true} existingInfo={null} />
    );
};

export default AddSkillPage;